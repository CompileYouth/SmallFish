import React, { Component } from 'react';

import {
  View,
  ListView
} from 'react-native';

export default class Application extends Component {
  render() {
    return (
      <ListView>
        <View />
        <View />
      </ListView>
    );
  }
}
